#include <stdio.h>
#include "calculations.h"

void firstCalculation(int a, int b) {
    printf("Sum = %d\n", a + b);
}
void secondCalculation(int a, int b) {
    printf("Product = %d\n", a * b);
}
